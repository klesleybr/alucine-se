$(document).ready(function(){
	$("#frases ul").bxSlider({
		auto: true, //modificadores da função, ver Options.
		speed: 400,
		pager: true, //Oculta a paginação.
        slideWidth: 800,
	});
  });